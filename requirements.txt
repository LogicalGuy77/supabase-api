fastapi==0.112.0
uvicorn[standard]==0.30.1
supabase==2.3.4
python-dotenv==1.0.0
pydantic==2.9.0

# Pydantic v2 + FastAPI 0.112.x selected for Python 3.13 compatibility.
# If Render still tries to build a pydantic-core wheel from source, bump pydantic to the latest patch.
